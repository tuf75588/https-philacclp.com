import React from 'react';
import Layout from '../components/layout';
import Container from '../components/container';
import {css} from '@emotion/core';
function About() {
  return (
    <Layout>
      <Container>this will be the about page</Container>
    </Layout>
  );
}

export default About;
